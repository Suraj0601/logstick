import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
    images: {
      domains: [
        "files.cdn-files-a.com",
        "cdn-cms-s-8-4.f-static.net",
      ],
    },
}

export default nextConfig;
